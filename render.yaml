################################################################################
# Render Platform Configuration
################################################################################
# This file defines the infrastructure for deploying the Fleet Management
# System to Render cloud platform.
#
# Services:
#   - Web service (Docker-based Kotlin/Ktor application)
#   - PostgreSQL database (managed)
#   - Redis cache (managed)
################################################################################

services:
  # Main API Web Service
  - type: web
    name: fleet-management-api
    env: docker
    dockerfilePath: ./Dockerfile
    
    # PLAN OPTIONS:
    # - free: $0/month (spins down after 15 min inactivity, 750 hrs/month limit)
    # - starter: $7/month (always on, no spin down)
    # - standard: $25/month (1GB RAM, 0.5 CPU)
    # 
    # ⚠️ FREE TIER LIMITATIONS:
    # - Spins down after 15 minutes of inactivity
    # - First request after spin down takes 30-60 seconds (cold start)
    # - 750 free hours per month (good for testing/development)
    # - Shared CPU (slower performance)
    # 
    # ✅ FREE TIER WILL WORK for development/testing
    # 💵 Upgrade to STARTER ($7/mo) for production (always on)
    plan: free  # Change to "starter" for production

    
    # Environment Variables
    envVars:
      # Port (injected by Render)
      - key: PORT
        value: 10000
      
      # Database connection (Manually set in Render Dashboard to Supabase)
      - key: DATABASE_URL
        sync: false # Do not overwrite if set in dashboard
      
      # Redis connection (from managed Redis)
      - key: REDIS_URL
        fromService:
          name: fleet-management-redis
          type: redis
          property: connectionString
      
      # JWT Configuration
      - key: JWT_SECRET
        generateValue: true  # Render auto-generates secure random value
      
      - key: JWT_ISSUER
        value: fleet-management-api
      
      - key: JWT_AUDIENCE
        value: fleet-management-users
      
      - key: JWT_EXPIRATION
        value: 3600000  # 1 hour in MILLISECONDS
      
      # Application Environment
      - key: APP_ENV
        value: production
      
      # Database Pool Configuration
      - key: DB_POOL_SIZE
        value: 10
      
      # Logging
      - key: LOG_LEVEL
        value: INFO
    
    # Health Check Configuration
    healthCheckPath: /health
    
    # Auto-deploy on git push
    autoDeploy: true
    
    # Build configuration
    buildFilter:
      paths:
        - src/**
        - build.gradle.kts
        - Dockerfile
        - gradle/**

  # Redis Cache
  - type: redis
    name: fleet-management-redis
    plan: free  # $0/month (persistent)
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow from anywhere (for testing)


# ⚠️ Managed Databases Removed (Moved to Supabase)

